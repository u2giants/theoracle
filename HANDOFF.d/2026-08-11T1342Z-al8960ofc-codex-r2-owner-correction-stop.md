# HANDOFF: R2 local owner correction stopped at 26/30

Created 2026-08-11 13:42 UTC on `al8960ofc` by Codex.

## 0. Decisions only the owner can make

The bounded local correction stopped honestly at 26/30, below the frozen 27/30 line. Albert must
decide whether to authorize a new written design for the four residual rows or reject the local
diff. Recommendation: do not land the present diff by itself. First decide whether the general
numbered-inner-actor case in rows 14 and 29 merits a new bounded plan. No production, deployment,
commit, push, or C5 review is authorized from the current result.

Already settled: do not weaken the verifier, add a source-prefix lookup, add fixture vocabulary,
raise budgets, change models, enable merge/apply, deploy, run production, or consume the frozen gate.

## 1. What this application is

The Oracle is POP Creations and Spruce Line's evidence-backed company knowledge system. Employees
upload documents and ask business questions. Workers build exact source-bound duties and reviewable
business maps. The repo is `C:\repos\oracle`, GitHub `u2giants/theoracle`, branch `main`. The web
app is `https://oracle.designflow.app`; workers use Trigger.dev project
`proj_wgpzsvhmsopqhvwqaycn`; data uses Supabase project `eqccjfbyrywsqkxxpjvg`.

## 2. What we set out to do, and why

Albert explicitly authorized executing C0-C5 of `plan_r2_local_owner_context_correction.md`. The
goal was to correct general source-bound owner propagation after the honest P5 verifier exposed a
16/30 result. The fixed contract required at least 27/30 with the unchanged exact-span verifier,
no regression of the original sixteen supported rows, and a hard stop below 27.

## 3. Current state

- C0 reproduced 16/30 with the expected fourteen unsupported rows.
- C1 added typed actor context, descriptive-heading rejection, bounded actor carry-through, blank
  narrative and markdown reset behavior, and exact evidence preservation.
- C2 added direct-duty-subject precedence over an outer label, recipient safety, ambiguous no-guess
  behavior, and `outer_actor_overridden` audit data.
- C3 requires split children to have a proven actor, one duty action, and a non-empty source object;
  incoherent splits remain one `ambiguous_multi_verb` parent.
- Generic `verify:r2-responsibilities` passes.
- The unchanged pinned verifier reports **26/30 from 139 source-bound seeds**. Supported rows are
  1-13 except 14, plus 15, 17-25, 27, 28, and 30. Unsupported rows are 14, 16, 26, and 29.
- Every one of the sixteen baseline-supported rows remains supported.
- C4 therefore failed honestly. C5, independent review, landing, commit, push, CI, deployment, and
  production were not run.
- Local branch is `main`; `origin/main` remains at `85dab98`. The implementation and docs are
  uncommitted. Unrelated untracked `.ai`, `.playwright-cli`, screenshot, and image files remain
  untouched.

Changed deliberate files:

- `apps/workers/src/lib/responsibility-reader.ts`
- `apps/workers/src/__verify__/r2-responsibility-reader.ts`
- `plan_r2_local_owner_context_correction.md`
- `plan_r2_source_span_inventory_reader.md`
- this handoff

## 4. Everything we tried that did not work

The approved owner correction predicted eleven recoveries with zero margin. It recovered ten. The
direct-subject helper works for `[Outer Actor] Inner Actor reviews ...`, but the real residual rows
14 and 29 contain an intervening list marker: `[Outer Actor] 1. Inner Actor ...`. The current helper
does not cross that marker. The C4 stop rule forbids adding that extra parser rule in this session.

Row 16 still splits review and ensure into two individually coherent children, but neither one
provides the unique expected review assignment with every object anchor. Row 26 remains only
`[Licensed Team] Submit the Reports`, without source-bound quarterly royalty object words. No
adjacent heading or wider-span bridge was attempted.

## 5. Root causes and key findings

The original false actor `the following scenarios` is corrected generally. Descriptive bracket and
colon labels no longer occupy or clear the actor slot. Direct actors already visible inside a duty
can override a conflicting outer label, and the override is audited.

The remaining score gap is not a regression. It is one unimplemented general syntax shape across
two rows, plus two already known split/topic-context residuals. The frozen no-bridge rule is doing
its job: a plausible one-line parser extension cannot be smuggled in after measurement.

## 6. Exact next steps

1. Read `AGENTS.md`, `HANDOFF.md`, this handoff, both R2 plans, and the local diff. Success: the
   status says 26/30 and C5 blocked.
2. Ask Albert whether to reject the diff or authorize a new written, independently reviewed plan
   for numbered inner actors after outer labels. Success: the decision is dated in both plans.
3. If rejected, remove only this session's four tracked-file edits with a safe reviewed patch; do
   not touch unrelated untracked files. Success: `git diff` contains no session changes.
4. If a new plan is authorized, start from the exact four-row matrix and preserve the C4 no-bridge
   boundary until the new plan is independently approved. Success: no implementation begins from
   chat inference alone.
5. C5 and landing may run only after an unchanged score of at least 27/30. Success: all section 10
   checks pass, a fresh review has no P0/P1 issue, and only then are exact files committed/pushed.

## 7. Constraints and gotchas

Work only on `main`; use `apply_patch`; stage exact files only; verify Albert's git identity before
any future commit. Preserve unrelated `.ai`, `.playwright-cli`, `detail-current.png`, and
`rfq-before.png`. Do not edit the pinned verifier, answer key, threshold, fixture SHA, budgets,
models, prompts, database, merge/apply settings, deployment, or production. Root `HANDOFF.md` is a
static pointer and must not be rewritten.

## 8. Access and environment

Windows checkout `C:\repos\oracle`; pnpm from repo root; GitHub CLI authenticated. Commit identity
was verified as `Albert Hazan <u2giants@users.noreply.github.com>`. The pinned local fixture is
`Z:\Documentation\company process - Oracle\Licensed Team Responsibilities 2 - tagged.txt`.
Secrets remain in 1Password vault `vibe_coding`; none were read or changed.

## 9. Open questions and risks

- Owner decision: reject the uncommitted 26/30 diff or authorize a new bounded design.
- Risk: landing a useful but below-gate parser change would bypass the precommitted acceptance rule.
- Risk: adding optional list-marker support without a new plan would violate the explicit C4 stop,
  even though rows 14 and 29 make that change look attractive.
- Risk: eight open handoff files now exist. Do not delete another session's file without proof its
  workstream is complete.

## Mandatory self-audit

Passed. A street-new developer can identify the application, goal, exact diff, honest score,
supported and unsupported rows, failed path, root cause, next owner decision, executable next
steps, constraints, access, and release prohibition without this chat. Every next step has a
verification gate, failures are preserved, and no secret value appears.
